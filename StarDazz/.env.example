# Canonical site origin (no trailing slash). Used for sitemap.xml, robots.txt,
# and as fallback when window is unavailable. In Vercel, set the same name under Project → Environment Variables.
# VITE_SITE_URL=https://stardazz-com.vercel.app